export default function Home() {
  let title = "Build full-stack apps with plain JavaScript."
  return `
    <section class="hero">
      <p class="eyebrow">FastScript</p>
      <h1>${title}</h1>
      <p>Simple syntax, fast compiler pipeline, deploy anywhere.</p>
    </section>
  `;
}
