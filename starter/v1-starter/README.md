# Velox App

Template: `pages`

## Commands

```bash
npm install
npm run dev
npm run build
npm run check
```

## Registry Smoke Command

From any clean directory, you can also compile a `.vx` file directly from npm:

```bash
npx -y @lakesbim/velox build ./path/to/file.vx -o ./out.js
```
