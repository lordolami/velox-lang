@fast speed_ratio(numerator: i32, denominator: i32) -> i32 {
  if denominator == 0 {
    return 0
  }
  return numerator / denominator
}

@fast bundle_delta(base: i32, current: i32) -> i32 {
  return base - current
}
