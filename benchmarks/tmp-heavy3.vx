@fast heavy3(n: i32) -> i32 {
  let i = 1
  let acc = 0
  while i < n {
    let a = i * i
    let b = (i + 17) * (i + 3)
    acc = acc + (a % 4093)
    acc = acc + (b % 8191)
    i = i + 1
  }
  return acc
}