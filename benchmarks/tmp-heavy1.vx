@fast heavy1(n: i32) -> i32 {
  let i = 1
  let acc = 0
  while i < n {
    acc = acc + ((i * 31) % 997)
    acc = acc + ((i * i) % 1021)
    acc = acc + ((i * 7) % 89)
    i = i + 1
  }
  return acc
}